/*
package src.main.java.org.example.base;

//check if two array contains duplicate

import java.util.HashSet;
import java.util.Set;

public class Newjavaclass {

    public static void main(String[] args) {

        String [] str1 = {"apple", "apple", "banana", "banana", "orange", "orange"};
        int[] num2 = {10, 40, 70, 80};
        System.out.println(duplicate(num1));
        System.out.println(duplicate(num2));

    }

    public static boolean duplicate( int[] nums) {

        Set<Integer> seen = new HashSet<>();
        for (Integer num : nums) {
            if (seen.contains(num)) {
                return true;
            }
             seen.add(num);
        }
        return false;
       }















*/
